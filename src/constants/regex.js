export const REGEX = {
    REGEX_EMAIL: /^[\w-.]+@([\w-]+\.)+[\w-]{2,4}$/,
    REGEX_PASSWORD: /^[a-zA-Z0-9]+$/,
    REGEX_STRING: /^(?!^\s+$)\s*\S.*\S\s*$/,
    REGEX_PHONE: /^\+?\d{1,3}[-.\s]?\(?\d{1,3}\)?[-.\s]?\d{1,4}[-.\s]?\d{1,4}$/
}